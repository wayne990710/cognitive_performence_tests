# 2-back 工作記憶測驗：開發進度

最後更新：2026-09-11（第二次）

本文件記錄 2-back 測驗併入現有 Stroop 施測程式的開發進度、已確定的決策，
以及尚待決定的事項。研究背景為高中專題「高中教室二氧化碳濃度對學生認知表現影響之預測模型」。

---

## 一、現有程式架構（動工前的理解）

程式是一支 Flask + Flask-SocketIO 的「Kahoot 式」同步測驗，只有三個核心檔案：

| 檔案 | 作用 |
|---|---|
| `app.py` | 伺服器：路由、socket 事件、CSV 匯出 |
| `templates/client.html` | 學生手機端：登入、等待、測驗、結束畫面 |
| `templates/admin.html` | 主控端：連線人數、開始測驗、匯出資料 |

運作流程：

1. 學生連 `http://{ip}:5000`，輸入 6 碼班級座號，`emit('join')` 進入等待室。
2. 研究人員在 `http://{ip}:5000/admin` 按「開始測驗」，觸發 `start_test`。
3. 伺服器廣播 `test_started`，所有手機同時進入測驗。
4. **題目序列、計時與計分全部在瀏覽器端執行**（`generateRealTrials()`、`Date.now()` 算反應時間）。
5. 8 題練習（有對錯回饋）→ 24 題正式題（中性／一致／不一致各 8 題）。
6. 跑完才一次性 `emit('submit_result')` 把整包結果送回伺服器。
7. 伺服器把結果存在記憶體的 `test_results`，等研究人員按「匯出資料」才寫成
   `output/stroop_kahoot_YYYYmmdd_HHMMSS.csv`。

現有 CSV 欄位：`Student_ID, Trial, Condition, Reaction_Time_ms, Is_Correct`

---

## 二、已確定的決策

開發規格文件中有數處與現有程式的實際狀況不符，經確認後決定如下：

### 1. 兩個測驗合併成一支程式，放在 `stroop_project`

原因：規格要求「Stroop 結束後自動進入 2-back 的說明頁」。若分成兩支獨立程式、
各自一個 port，學生做完 Stroop 必須自己換網址、重新輸入代碼，主控端也要再按一次開始，
同步會散掉，且同一人在兩個測驗的資料需要靠代碼手動拼回去。
合併後學生只輸入一次代碼，兩邊 CSV 天然共用同一組代碼與同一個場次。

`Memory_test` 資料夾不使用。

### 2. 兩個測驗都記錄 ISO 8601 時間戳記

現有 Stroop 的 CSV **完全沒有時間欄位** —— 檔名上的 `20260810_160422` 是按下匯出鍵的時刻，
不是學生作答的時間，而且全班共用一個，因此 Stroop 現有資料無法精確對齊 CO2 曲線。

決定：2-back 記錄 ISO 8601 含毫秒的時間戳記（例如 `2026-09-11T14:23:05.478`），
**並回頭替 Stroop 補上同格式的欄位**，使兩個測驗的資料都能對上 AirBox 時間軸。
改動 Stroop 後需重新實測一次。

### 3. `session_slot`（當日第幾次施測）由主控端啟動時選擇

現有程式沒有這項設定可以沿用。決定在 `/admin` 按開始前，與「第一次施測／一般施測」
一起選擇本次是當日第 1 次還是第 2 次，由伺服器統一寫入所有人的資料，不依賴學生操作。

### 4. 每題即時回傳伺服器並立刻落檔

現有 Stroop 的結果只存在瀏覽器記憶體，跑完才送出：中途重新整理或斷線該生資料全失，
伺服器端也只放在記憶體變數（`debug=True` 自動重載也會清空）。

決定：2-back 每答完一題就 `emit` 一筆到伺服器，並立即附加寫入該次施測的 CSV。
手機重整或斷線最多只掉當下那一題，伺服器重啟也不影響已落檔的資料。

### 5. 序列在伺服器端用 Python 生成

Stroop 的題目序列是在手機端的 JavaScript 生成的，但規格要求序列生成需附單元測試，
並要能連續生成 1,000 組做統計，因此 2-back 的序列改由伺服器端的 Python 生成後發給各手機。
這也配合「每題即時回傳」——伺服器本來就需要知道每題是什麼。

其餘（路由、頁面樣式、主控端啟動方式、6 碼代碼輸入、CSV 輸出位置）全部沿用 Stroop。

---

## 三、第一階段：序列生成與單元測試（已完成）

| 檔案 | 作用 |
|---|---|
| `twoback_sequence.py` | 序列生成，全部序列規則都在這裡 |
| `test_twoback_sequence.py` | 27 個單元測試 |
| `twoback_sequence_check.py` | 連續生成 1,000 組序列的分布統計 |

### 測試方式

在 `stroop_project` 資料夾下開終端機：

```
python -m unittest test_twoback_sequence -v
python twoback_sequence_check.py
```

### 測試結果

- 27 個單元測試全部通過。
- 1,000 組序列**每一組都是恰好 9 題目標題**，全部符合全部序列規則。
- 目標題位置落在第 3～30 題，每個位置約 300～340 次（期望值 321.4），
  沒有集中於特定位置，受試者無從猜測規律。
- 12 個字母的使用次數平均（30,000 題中每個字母約 2,400～2,600 次）。

### 已實作的序列規則

1. 只使用 12 個子音字母：B、F、H、J、K、L、M、N、R、S、T、X
2. 正式測驗 30 題，目標題恰好 9 題
3. 第 1、2 題不可為目標題
4. 非目標題不可與前一題相同（避免 1-back 誘答），也不可與前兩題相同
5. 目標題位置隨機，每次施測重新生成
6. 練習 12 題／目標 4 題、暖身 6 題／目標 2 題，套用同一組規則

### 兩點已確認

- **額外加了一條規格沒要求的規則**：整份序列不會出現連續兩個相同字母
  （否則畫面上會連閃兩個一樣的字，看起來像當機）。**已確認保留。**
- **目標題可能連續出現**（例如第 17、18 題都是目標題）。規格未禁止故未禁止，
  **已確認維持現狀**（相鄰目標題在 2-back 研究中是常見且可接受的設計）。

---

## 四、原待決定事項（2026-09-11 已全數確認）

1. **自覺疲勞量表**：**另外收，與本程式無關。**
   與研究計畫書一致——計畫書寫明量表以「線上表單」收發，每日兩次、下課後約 1 分鐘填寫。
   因此本程式不需要處理量表，2-back 的說明頁直接接在 Stroop 結束畫面之後即可。

2. **Stroop 的成績顯示**：**保留，不關掉。**
   理由是成績顯示能讓受試學生有參與感。受試者是連續 10 個上課日、每日兩次的自願參與者，
   持續配合的意願本身就是資料品質的前提。
   （2-back 本身仍依倫理審查不顯示任何成績，只是不再連帶關掉 Stroop 的。）

3. **序列不出現連續相同字母的額外規則**：**保留。**

4. **目標題是否可相鄰**：**維持現狀，不禁止。**

---

## 四之二、規格權威來源：清大 REC 第 1 次計畫變更案

規格以 `C:\Users\user\OneDrive\文件\清大 REC 繳交檔\變更案\` 內的送審文件為準
（REC 編號 11507ES324，版本日期 2026-09-08）。桌面上那份
`許博渭_專題研究_...docx` 尚未依變更案更新，**不可用來判斷程式規格**。

主要文件：

| 檔案 | 內容 |
|---|---|
| `03 2-back 工作記憶測驗說明 20260908.pdf` | 2-back 完整規格與施測流程 |
| `02 變更前後對照表.pdf` | 17 項逐條變更 |
| `04 變更後-研究計畫書 / 知情同意書 / 招募海報 / 新案審查申請書` | 變更後版本 |

### 送審規格與目前實作的對照

| 規格項目 | 送審內容 | 目前狀態 |
|---|---|---|
| 刺激材料 | 大寫子音字母，排除母音與形近字母，共 12～15 個 | 已實作 12 個 |
| 刺激時序 | 每題 2.5 秒（顯示 500 ms、空白 2,000 ms） | 待第二階段 |
| 題數 | 正式 30 題，目標題 9 題（約 30%），前 2 題不設目標題 | 已實作 |
| **不設誘答題** | 明文寫入送審文件 | 已實作（1-back 與 3-back 誘答皆排除） |
| 作答方式 | 相同時點擊單一按鈕，不同時不作反應 | 待第二階段 |
| 練習與暖身 | 第一次 12 題練習、其後每次 6 題暖身，**兩者皆每題顯示對錯回饋**，不列入分析 | 序列已實作；回饋待第二階段 |
| 單次時間 | 正式約 75 秒，含暖身約 90 秒 | — |
| 施測時機 | 每日 2 次，接續在自覺疲勞量表與 Stroop 之後 | 待第二階段 |
| 記錄指標 | 每題反應與毫秒級反應時間；命中率、誤報率、**d'（log-linear 校正）**、命中反應時間，以 d' 為主要指標 | 待第三階段 |
| 結束畫面 | 顯示「完成」，**不顯示成績**，避免比較壓力 | 待第二階段 |
| 中途退出 | 研究對象可隨時放下裝置停止作答，不需說明理由 | 每題即時落檔的設計已能支援 |
| 資料輸出 | 測驗結束後自動匯出 CSV，不含姓名等可辨識資訊 | 待第三階段 |

### 「不設誘答題」的補實作（2026-09-11 已完成）

送審文件在「題數」一欄明文寫「**不設誘答題**」。n-back 的誘答題（lure）
指的是與 n±1 個位置前相同、會誘使受試者誤按的非目標題。對 2-back 而言有兩種：

- **1-back 誘答**：與前 1 題相同，原本就已排除。
- **3-back 誘答**：與前 3 題相同，原本沒有排除，**已補上**。

`twoback_sequence.py` 的非目標題選字已再多排除 `sequence[i-3]`，
排除字母數由最多 2 個增為最多 3 個，候選字母仍有 9 個以上，不影響可生成性。
`validate_sequence()` 也已能偵測 3-back 誘答，並補上兩個單元測試（共 27 個，全數通過）。
重跑 1,000 組分布檢查：全部合規，目標題位置仍平均（296～352 次，期望值 321.4），
12 個字母使用次數仍平均（每個約 2,450～2,560 次）。

### 送審文件已回答、先前列為疑慮的兩件事

- 施測時間已在變更案中更新為「單次約 3 至 3.5 分鐘、每日約 6 至 7 分鐘」
  （對照表第 4、13、14、16 項），知情同意書與招募海報都已改。先前擔心的超時問題不存在。
- 計畫書、新案審查申請書、知情同意書、招募海報、研究材料保管及銷毀同意書
  皆已補入 2-back（對照表第 1～17 項）。

### 兩點需要留意（非阻擋，供研究者判斷）

1. **資料蒐集期間為 2026/09/17 至 2026/10/02**（對照表第 3、9 項）。
   程式必須在 9/17 前完成並實測完畢。
2. **時間戳記對齊的區間未隨變更案更新**。計畫書原文寫「Stroop 測驗以測驗進行的
   40 秒區間為基準」擷取 CO2 平均值，變更案未新增 2-back 的對齊區間規則。
   由於本程式會逐題記錄 ISO 8601 毫秒級時間戳記，兩個測驗要各自取區間、
   或合併取一個約 3 分鐘的區間，分析階段都做得到，屬研究者的分析設計決定。

---

## 五、第二階段：前端流程與伺服器（已完成，已實測）

### 完成的內容

| 檔案 | 改了什麼 |
|---|---|
| `app.py` | 場次設定、序列發送、逐題落檔、時鐘校正、兩個 CSV |
| `templates/client.html` | Stroop 結束畫面、2-back 說明頁、字母呈現、「相同」按鈕、完成畫面 |
| `templates/admin.html` | 施測類型與場次選擇、本場資料檔案路徑、分階段進度監控 |

### 學生端的完整流程

1. 輸入 6 碼班級座號進入等待室。
2. 主控端按開始 → Stroop 8 題練習（有回饋）→ 24 題正式。
3. **Stroop 結束畫面**：顯示正確率、平均反應時間、全班排名，
   倒數 20 秒後自動進入 2-back 說明頁（也可按鈕直接進入）。
4. **2-back 說明頁**：規則說明、`K S K M T M` 圖示範例、
   「成績不影響在校成績、隨時可以停止作答」，倒數 30 秒後自動開始。
5. **練習／暖身**：第一次施測 12 題、其後 6 題，每題顯示對錯回饋
   （按了有回饋，沒按的題目在該題結束時也會補上回饋）。
6. **正式測驗 30 題**：不顯示對錯，只在按下時把按鈕變深色表示「已接受」。
7. **完成畫面**：只有「完成！」，不顯示任何成績。

### 時間戳記怎麼對齊 CO2

各支手機的系統時間可能差好幾秒，直接用手機時間會對不上 AirBox 的時間軸。
作法是學生一進等待室就與伺服器做 5 次時間校正，取來回延遲最小的一次，
以「伺服器時間 + 延遲的一半」推算偏移量；之後每題的刺激呈現時刻都用校正後的時間，
再由伺服器統一格式化成 ISO 8601 字串（避免某支手機時區設錯）。
實測在區域網路下來回延遲約 2 毫秒。

### CSV 欄位

兩個檔案都在按下「開始測驗」時建立，之後逐筆附加寫入。

`output/stroop_YYYYmmdd_HHMMSS.csv`

```
Session_ID, Session_Slot, Practice_Type, Student_ID, Attempt,
Trial, Condition, Stimulus_Onset, Reaction_Time_ms, Is_Correct, Page_Hidden
```

`output/twoback_YYYYmmdd_HHMMSS.csv`

```
Session_ID, Session_Slot, Practice_Type, Student_ID, Attempt,
Phase, Trial, Letter, Is_Target, Responded, Response_Type,
Is_Correct, Reaction_Time_ms, Stimulus_Onset, Page_Hidden
```

`Response_Type` 為命中／漏失／誤報／正確拒絕，第三階段算 d' 直接數這一欄即可。
`Phase` 為練習／暖身／正式，練習與暖身依規格不列入分析，但仍然記錄下來，
可用於檢查該名學生是否真的理解規則。

### 實測結果（2026-09-11）

以瀏覽器自動作答跑完整流程，一般施測與第一次施測兩種都測過：

- 正式測驗恰好 30 題、目標題恰好 9 題，序列規則檢查全部通過。
- 刺激間隔 2.501～2.517 秒（規格 2.5 秒），正式測驗總長 75.2 秒（規格約 75 秒）。
- 命中、漏失、誤報、正確拒絕四種反應都正確記錄。
- 時間戳記為 ISO 8601 含毫秒，例如 `2026-09-11T22:51:17.970`。
- 連續跑兩場（結束本場 → 開始下一場），第二場的狀態會完整重設。

### 斷線重連的處理

規格允許「研究對象可隨時放下裝置停止作答」，加上手機本來就可能斷線，
因此做了以下處理：

- 2-back 的序列以 **班級座號**（不是連線 id）為 key 保存在伺服器端，
  手機重新整理後拿回同一組序列，不會換一套題目。
- 已答完的題目都已經寫進 CSV，重連最多只掉當下那一題。
- 重連的學生會被重新帶回 Stroop 開頭。為了避免同一位學生在同一場的 CSV 裡
  出現兩組同題號的 Stroop 資料，伺服器**只採計每位學生在該場第一次送回的 Stroop 成績**。
- 2-back 的逐題資料沒有做這層去重，若有學生中途重連又重跑，
  該場會出現兩組 2-back 資料，可用 `Stimulus_Onset` 分辨先後，第三階段算摘要時需注意。

### 實測時修掉的三個問題

1. **按「直接進入記憶測驗」會讓測驗中途被拉回說明頁（會毀掉整場資料）**。
   Stroop 結束畫面有一個 20 秒倒數，時間到會自動進入 2-back 說明頁。
   學生若提早按按鈕先進去，**那個倒數計時器並沒有被取消**，
   20 秒後照樣再觸發一次轉場，把畫面從進行中的 2-back 拉回說明頁；
   題目卻仍在背後照常一題一題跑完，學生看不到也按不到，
   於是 20 秒之後的每一題都被記成「漏失」。

   這是實測時最嚴重的一個問題，而且觸發條件正是那顆按鈕存在的目的。
   修法是替流程加上一個只能往前走的階段旗標
   （`stroop → stroopDone → tbIntro → tb → done`），
   所有轉場都必須通過 `enterStage()`，任何回頭的轉場一律被擋掉；
   `goToTwobackIntro()` 同時會把 Stroop 的倒數計時器收掉。

   修好後重測：學生一到 Stroop 結束畫面就馬上按按鈕、說明頁也馬上按開始，
   全程 0 次畫面跳動，9 題目標題全部命中、21 題正確拒絕。

2. **同一支手機連跑兩場時，2-back 的狀態沒有重設**，會導致第二場直接跳過測驗。
   已在 `test_started` 時呼叫 `resetForNewSession()` 清乾淨所有狀態與計時器。

3. **改完 HTML 不重啟伺服器不會生效**。因為正式施測不開 `debug`，
   Flask 預設會快取樣板。已加上 `TEMPLATES_AUTO_RELOAD = True`。

### 怎麼啟動

在 `stroop_project` 資料夾下開終端機，輸入：

```
python app.py
```

啟動後畫面會**直接印出學生端與主控端的網址**，不需要自己去查 IP：

```
========================================================
  認知測驗伺服器已啟動
========================================================

  學生端（請學生用手機瀏覽器開啟）
      http://10.0.0.17:5000

  主控端（研究人員自己開）
      http://10.0.0.17:5000/admin
```

IP 的取得方式是開一個 UDP socket 去「連」一個外部位址，
再問作業系統實際用了哪張網卡；UDP 的 connect 不會真的送出封包，
所以教室沒有對外網路也問得出來。若電腦同時有 Wi-Fi、有線網路或 VPN 的網卡，
畫面會把其他位址一併列出來備用。

若忘記前一個視窗還開著又執行一次，不會跳出一長串錯誤，
而是直接顯示「連接埠 5000 已經被占用」並說明該怎麼處理。

### 主控端的操作

1. 開畫面上印出的主控端網址。
2. 選「施測類型」：整個研究期間只有第一天的第一場選「第一次施測」，其餘選「一般施測」。
3. 選「當日場次」：第 1 次或第 2 次。
4. 確認連線人數後按「開始測驗」。選項會鎖住，避免施測中被誤改。
5. 跑完後按「結束本場」，才能開始下一場。**資料已經即時寫入檔案，不需要匯出。**

---

## 六、破壞性測試（2026-09-12）

為了確認正式施測時不會壞掉，刻意用各種奇怪的方式操作程式。
測了 24 種畸形的 socket 輸入、登入欄位的 16 種爛輸入、狂點按鈕、
測驗中重新整理、測驗中斷線、同一個座號多人登入、主控端連按、
手機切到背景，以及 25 人同時上線跑完整場次。

### 沒問題的部分

- **25 人同時上線**（實際受試人數）跑完整場次：Stroop 600 筆、2-back 900 筆，
  25 個人的資料全部完整，CSV 沒有任何壞掉的列。
- **狂點「相同」按鈕**：每題連點 8 下，只會記錄第一下，單次點擊鎖定有效。
- **主控端連按 10 次開始測驗**：只會開出一場。
- **伺服器不會被打掛**：24 種畸形輸入跑完，伺服器仍正常運作。

### 修掉的八個問題

1. **伺服器 handler 會被畸形輸入打斷，而且現場完全看不出來。**
   `data.get()` 遇到非 dict、`r['correct']` 缺欄位、反應時間是文字時都會拋例外。
   例外會被 socketio 接住，伺服器不會掛，但**那位學生的資料就安靜地沒被寫進檔案**。
   已加上 `as_dict()` 等防呆，所有 handler 都容忍畸形輸入。

2. **登入驗證太鬆。** 原本用 `length===6 && !isNaN()` 判斷，
   會放行 `-11459`、`+11459`、`" 11459"`（前面有空白）、`1e5000`、`0x1234`、`00.000`。
   這些都不是有效的班級座號，收進去就對不回編碼對照表，那位學生該場的資料等於白做。
   已改成只接受剛好 6 個半形數字。

3. **CSV 公式注入。** 開頭是 `=` `+` `-` `@` 的欄位會被 Excel 當公式執行，
   研究人員打開 CSV 時會看到錯誤或被執行的內容。已在寫入時於前面補單引號。

4. **手機切到背景或鎖屏時，刺激時序會被瀏覽器拉長，而且看不出來。**
   瀏覽器會把背景分頁的計時器壓到最少 1 秒，實測暖身題的間隔從 3.2 秒變成 4.0 秒，
   字母的 500 毫秒呈現也會被拉長。學生當下根本看不到畫面，
   那幾題等於空白作答，卻會被當成正常的「漏失」算進 d'。
   已新增 **`Page_Hidden`** 欄位，逐題記錄該題進行時畫面是否在背景
   （題目開始時就在背景、或中途被切走，都會標成「是」），分析時必須排除。

5. **斷線重連後，資料會全部變成「未知」。** socket.io 重連會拿到新的連線編號，
   伺服器若不重新對應就認不得這位學生。已加上 `rejoin` 事件，
   重連時重新表明身分；刻意不發 `test_started`，否則會把正在進行的測驗重設回 Stroop 開頭。
   學生端也加上紅色的「連線中斷」提示條。

6. **重複的班級座號沒有任何提示。** 兩位學生打錯成同一個號碼、
   或同一個人用兩支裝置登入時，資料會混在同一個座號底下而分不出來。
   主控端現在會把重複的座號標紅並跳出警告，開始測驗前就看得到。

7. **每次誤按開始都會留下一對空的 CSV。** 改成第一筆資料寫入時才建檔，
   沒有資料的場次不會留下檔案。25 人併發實測確認不會寫出兩次欄位列。

8. **按「結束本場」後，場次資訊框沒清掉**，還顯示上一場的檔案路徑，
   容易誤以為還在進行中。已改成顯示「上一場已結束」。

### 新增的 `Attempt` 欄位

學生若在測驗中重新整理，會被帶回 Stroop 從頭再做一次。
原本的作法是「同一場只採計第一次的 Stroop 成績」，但這樣在 2-back 會拼出
第一輪的前半段加第二輪的後半段，反而更糟。
改成**兩輪都留著**，用 `Attempt` 欄位標示是第幾次嘗試，
分析時取最後一次完整的即可，也才看得出現場發生過什麼事。

### 仍然存在、需要現場注意的兩件事

- **測驗中重新整理會從 Stroop 從頭再做一次**，多花約 3 分鐘。
  資料不會遺失（靠 `Attempt` 區分），但學生的時間負擔會超出 REC 核定的 3～3.5 分鐘。
  施測前要提醒學生測驗中不要重新整理、不要切到其他 App。
- **伺服器若在場次進行中重開**，該場的設定會消失，
  之後送回的資料會被丟掉。跑測驗時不要去動伺服器的視窗。

---

## 七、版本控制

遠端：`https://github.com/wayne990710/cognitive_performence_tests.git`

`.gitignore` 已排除 `output/` 與所有 `*.csv`。
**這個 repo 是公開的**，而施測資料含 6 碼班級座號，
依 REC 核定之研究材料保管方式只能存放在學校帳號的受控空間，不可推上 GitHub。

---

## 八、後續階段

- **第三階段：CSV 摘要計算** —— 每位學生每場一列的摘要紀錄、
  log-linear 校正的 hit rate 與 false alarm rate、d'、命中題平均反應時間。

每階段完成後停下來實測再往下走。
